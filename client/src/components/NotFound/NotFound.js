import { Typography } from "@material-ui/core";
import React from "react";

const NotFound = () => {
  return <Typography variant="h6">No posts match the search!</Typography>;
};

export default NotFound;
